def coolFunc(a: int, b: int, c: int, d: int) : bool {
    _printf("params: %d  %d  %d  %d\n", a, b, c, d);
    var myChar: bool = true;
    return myChar;
}

var d: bool = coolFunc(1, 2, 3, 4);

_printf("d was: %s\n", d ? "true" : "false");

return 0;

